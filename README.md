# Q&A for when the fuckass project doesn't work

### Q: Why don't my changes take effect in the deployment?

**A**: Run ```npm run build``` before committing. 
Tailwind CSS, which is (unfortunately) used in the project needs to be run for GitHub pages to display the page correctly.  